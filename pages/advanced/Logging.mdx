## Logging 

### Log Events
Log events to automatically sent to a selected channel.


#### Log Bots
To log bots
#### Mod Roles

The mod roles are roles that will remember users events who have the role.

You can use the command `/modstats` to see how well your **Mods / Admins** are doing.


#### Channel
The selected Channel to send events to.
